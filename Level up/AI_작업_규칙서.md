# AI 코딩 어시스턴트 전용 작업 규칙서 (Stitch 템플릿 사용 프로젝트)

새로운 AI 대화창이나 새로운 프로젝트를 시작할 때, AI에게 이 파일을 읽어달라고 요청하시면 기존에 맞춰둔 호흡 그대로 빠르게 작업을 진행할 수 있습니다.

## 1. 파일 관리 규칙 (단일 파일 유지)
- `update_screens.ps1` 등 템플릿 다운로드용 PowerShell 스크립트는 다운로드 직후 **즉시 삭제**하여 실수로 코드가 덮어씌워지는 것을 방지한다.
- `*.png` 등 참고용 디자인 시안 이미지 파일들은 실제 웹 구동에 필요 없으므로 **전부 삭제**하여 파일 탐색기를 깔끔하게 유지한다.
- `dashboard_kr.html` 등 언어별 복사본이나 백업본은 작업에 혼선을 주므로, **메인 HTML 파일 하나만 남기고 전부 삭제**한다.

## 2. 디자인 및 CSS 덮어쓰기 규칙 (우선순위)
- 초기 뼈대는 Stitch(스티치) AI가 생성한 디자인 시스템과 공통 유틸리티 클래스(예: `p-margin-desktop`)를 따른다.
- 단, 이후 사용자의 요청에 의해 커스텀으로 들어가는 **새로운 레이아웃이나 간격 수정 작업이 무조건 1순위**가 되어야 한다.
- 기존 Stitch의 공통 클래스와 CSS 우선순위 충돌이 발생할 경우, **`!important` (예: `!pt-32`)를 적극 사용하거나 외부 여백(`mt-16`) 등 무시되지 않는 속성을 사용**하여 새로운 코드가 기존 코드를 완벽하게 제압(오버라이드)하도록 작성한다.

## 3. 사용자 확인 (Accept) 프로세스
- 모든 파일 수정은 IDE의 Diff(빨간줄/초록줄)를 통해 사용자에게 전송된다.
- 다중 파일을 수정했을 경우, 사용자가 각 파일 탭을 열어 모두 `Accept(수락)` 하도록 안내해야 한다.
